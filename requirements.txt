# Core LLM client (Ollama Python SDK, OpenAI-compatible fallback)
ollama>=0.4.0
httpx>=0.27.0

# Data & stats (CPU, no GPU)
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.13.0
scikit-learn>=1.4.0
statsmodels>=0.14.0

# RAG stack
chromadb>=0.5.0
sentence-transformers>=3.0.0
rank-bm25>=0.2.2
pypdf>=4.2.0

# Market data (free)
yfinance>=0.2.40

# Backtesting / portfolio math (pure Python, no C solvers required)
cvxpy>=1.5.0

# Writing pipeline
pydantic>=2.7.0
PyYAML>=6.0.1
jinja2>=3.1.4

# Dev / tests
pytest>=8.0.0
